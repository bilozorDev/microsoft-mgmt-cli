{
  "name": "m365-admin-cli",
  "version": "1.7.0",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run src/index.ts",
    "build:windows": "bun build --compile --target=bun-windows-x64 src/index.ts --outfile \"dist/Microsoft 365 Admin CLI/m365-admin.exe\" && cd dist && zip -r -9 \"Microsoft 365 Admin CLI.zip\" \"Microsoft 365 Admin CLI\""
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@clack/prompts": "^1.1.0",
    "exceljs": "^4.4.0"
  }
}
